package org.yeongtae.global.constants;

import org.yeongtae.global.Menu;

// MainMenu 열거형(enum)은 메인 메뉴의 항목들을 정의합니다.
public enum MainMenu implements Menu {
    MAIN, // 메인 화면
    JOIN, // 회원가입
    LOGIN, // 로그인
}
